<%_ 
  // performance test
  var totalMedia = 0,
      totalMediaTime = 0,
      totalText = 0,
      totalTextTime = 0,
      mesureStart, mesure;
  
  if (typeof debug !== 'undefined' && debug === true) {
    (function () {
      var time = null;
      mesureStart = function () {
        time = Date.now();
      }
      mesure = function () {
        return Date.now() - time;
      }
    } ())
  }
-%>
<% include channel_part_head.ejs %>
<% include channel_part_body.ejs %>
<% include channel_part_foot.ejs %>
<%_
  if (typeof debug !== 'undefined' && debug === true) {
    console.log(`
// channel.ejs
performance:
  totalText: ${totalText}
  totalTextTime: ${totalTextTime}
  totalMedia: ${totalMedia}
  totalMediaTime: ${totalMediaTime}
`)
  }
-%>