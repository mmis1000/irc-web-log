<%_ 
  // performance test
  var mesureStart, mesure;
  
  if (typeof debug !== 'undefined' && debug === true) {
    (function () {
      var time = null;
      mesureStart = function () {
        time = Date.now();
      }
      mesure = function () {
        return Date.now() - time;
      }
    } ())
    mesureStart();
  }
-%>
<% include channel_part_head.ejs %>
<% include channel_part_body.ejs %>
<% include channel_part_foot.ejs %>
<%_
  if (typeof debug !== 'undefined' && debug === true) {
    console.log(`
// channel.ejs
performance:
  totalTime: ${mesure()}
`)
  }
-%>